// node space enter

let msg = "welcome you all nodejs session"
msg
let num1 = 200, num2 = 50;

let out = num1 + num2
out


function square(num) {
    return num ** 2
}
square(5)

// then .save filename
// it load file in folder
